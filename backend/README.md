# Backend

Django-based backend for SmartDoorControl. Source currently lives at the repository root (`manage.py`, project apps, and templates). This folder documents backend-specific decisions and can host future API docs or deployment manifests.
